const Overlay = ({ children }) => {
  return <div className="w-screen h-screen p-0 m-0 bg-black ">{children}</div>;
};

// Overlay.propTypes = {
//   children: PropTypes.node.isRequired,
// };

export default Overlay;
